export interface IInvoice {
  title: string;
  fee: string;
  amount: string;
  color: string;
}
export interface IInvoiceActions {
  icon: React.ReactNode;
  title: string;
  subtitle: string;
  color: string;
  bgColor: string;
  subTitleColor: string;
  titleColor: string;
}
